<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ report_title }}</title>
</head>
<body style="font-family: Arial, sans-serif; margin:0; padding:20px; background:#f5f5f5;">
<div style="max-width:1200px; margin:0 auto; background:white; padding:20px; border-radius:8px;">
    <!-- HEADER -->
    <div style="text-align:center; color:#333; border-bottom:2px solid #e0e0e0; padding-bottom:15px; margin-bottom:30px;">
        <h1>{{ report_title }}</h1>
        <p>Generated on: {{ ansible_date_time.date }} {{ ansible_date_time.time }}</p>
    </div>

    <!-- SUMMARY CARDS -->
    <div style="display:flex; flex-wrap:wrap; gap:16px; margin-bottom:30px;">
        <div style="flex:1 1 200px; padding:20px; border-radius:6px; text-align:center; background:#28a745; color:white;">
            <h3 style="margin:0;">{{ report_data | selectattr('status', 'equalto', 'HEALTHY') | list | length }}</h3>
            <p style="margin:0;">Healthy Hosts</p>
        </div>
        <div style="flex:1 1 200px; padding:20px; border-radius:6px; text-align:center; background:#dc3545; color:white;">
            <h3 style="margin:0;">{{ report_data | selectattr('status', 'equalto', 'CRITICAL') | list | length }}</h3>
            <p style="margin:0;">Critical Hosts</p>
        </div>
        <div style="flex:1 1 200px; padding:20px; border-radius:6px; text-align:center; background:#17a2b8; color:white;">
            <h3 style="margin:0;">{{ report_data | map(attribute='healthy_containers') | list | flatten | length }}</h3>
            <p style="margin:0;">Running Containers</p>
        </div>
        <div style="flex:1 1 200px; padding:20px; border-radius:6px; text-align:center; background:#ffc107; color:#212529;">
            <h3 style="margin:0;">
            {{
                (report_data | map(attribute='down_containers') | list | flatten | length)
                + (report_data | map(attribute='missing_containers') | list | flatten | length)
            }}</h3>
            <p style="margin:0;">Problem Containers</p>
        </div>
    </div>
    
    {# NO ISSUES MESSAGE #}
    {% if (report_data | selectattr('status', 'equalto', 'CRITICAL') | list | length) == 0 %}
        <div style="text-align: center; color: #28a745; font-size: 18px; font-weight: bold; padding: 40px;">
            ✅ All AAP 2.5 components are healthy!<br>
            No missing or down containers detected.
        </div>
    {% endif %}

    {# HOST SECTIONS #}
    {% for host_data in report_data | sort(attribute='status', reverse=true) %}
    {% set banner_bg = '#28a745' if host_data.status == 'HEALTHY' else '#dc3545' %}
    <div style="margin-bottom:30px; border:1px solid #e0e0e0; border-radius:6px;">
        <div style="padding: 12px 15px; font-weight:bold; color:white; background:{{ banner_bg }};">
            <span>{{ host_data.host }}</span>
            <span style="float: right;">{{ host_data.component_type | title }} - {{ host_data.status }}</span>
        </div>
        {% if host_data.containers | length > 0 %}
        <table style="width:100%; border-collapse: collapse;">
            <thead>
                <tr>
                    <th style="padding: 10px; text-align:left; border-bottom: 1px solid #e0e0e0; background:#f8f9fa;">Container Name</th>
                    <th style="padding: 10px; text-align:left; border-bottom: 1px solid #e0e0e0; background:#f8f9fa;">Status</th>
                    <th style="padding: 10px; text-align:left; border-bottom: 1px solid #e0e0e0; background:#f8f9fa;">Start Time</th>
                </tr>
            </thead>
            <tbody>
                {% for container in host_data.containers %}
                <tr>
                    <td style="padding: 10px; text-align:left; border-bottom: 1px solid #e0e0e0;">{{ container.name }}</td>
                    <td style="padding: 10px; text-align:left; border-bottom: 1px solid #e0e0e0;">
                        {% if not container.present %}
                            <span style="background:#fff3cd; color:#856404; padding:4px 12px; border-radius:4px; font-size:12px; font-weight:bold;">Missing</span>
                        {% elif container.state == 'running' %}
                            <span style="background:#d4edda; color:#155724; padding:4px 12px; border-radius:4px; font-size:12px; font-weight:bold;">Running</span>
                        {% else %}
                            <span style="background:#f8d7da; color:#721c24; padding:4px 12px; border-radius:4px; font-size:12px; font-weight:bold;">{{ container.state | title }}</span>
                        {% endif %}
                    </td>
                    <td style="padding: 10px; text-align:left; border-bottom: 1px solid #e0e0e0;">
                        {% if container.present and container.started_at %}
                          {{ container.started_at | to_datetime | strftime("%Y-%m-%d %H:%M:%S") }}
                        {% else %}
                          N/A
                        {% endif %}
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% endif %}
    </div>
    {% endfor %}

    <div style="margin-top:30px; padding-top:20px; border-top:1px solid #e0e0e0; text-align:center; color:#666; font-size:12px;">
        Report generated by Ansible AAP 2.5 Container Health Check Playbook<br>
        Total Hosts Checked: {{ report_data | length }}
    </div>
</div>
</body>
</html>
