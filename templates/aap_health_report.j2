<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ report_title }}</title>
    <style>
        body { font-family: Arial, sans-serif; margin:0; padding:20px; background:#f5f5f5;}
        .container { max-width:1200px; margin:0 auto; background:white; padding:20px; border-radius:8px; }
        .header { text-align:center; color:#333; border-bottom:2px solid #e0e0e0; padding-bottom:15px; margin-bottom:30px;}
        .summary { display:grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-bottom:30px;}
        .summary-card { padding: 20px; border-radius:6px; text-align:center; color:white;}
        .healthy { background:#28a745; }
        .critical { background:#dc3545; }
        .warning { background:#ffc107; color:#212529;}
        .info { background:#17a2b8;}
        .host-section { margin-bottom:30px; border:1px solid #e0e0e0; border-radius:6px; overflow: hidden;}
        .host-header { padding: 12px 15px; font-weight:bold; color:white; }
        .host-header.HEALTHY { background:#28a745;}
        .host-header.CRITICAL { background:#dc3545;}
        .container-table { width:100%; border-collapse: collapse; }
        .container-table th, .container-table td { padding: 10px; text-align:left; border-bottom: 1px solid #e0e0e0;}
        .container-table th {background:#f8f9fa;}
        .status-badge { padding:4px 12px; border-radius:4px; font-size:12px; font-weight:bold;}
        .status-running { background:#d4edda; color:#155724; }
        .status-stopped { background:#f8d7da; color:#721c24; }
        .status-missing { background:#fff3cd; color:#856404; }
        .footer { margin-top:30px; padding-top:20px; border-top:1px solid #e0e0e0; text-align:center; color:#666; font-size:12px;}
        .no-issues { text-align: center; color: #28a745; font-size: 18px; font-weight: bold; padding: 40px;}
    </style>
</head>
<body>
<div class="container">
    <div class="header">
        <h1>{{ report_title }}</h1>
        <p>Generated on: {{ ansible_date_time.date }} {{ ansible_date_time.time }}</p>
    </div>

    {% set total_hosts = report_data | length %}
    {% set healthy_hosts = report_data | selectattr('status', 'equalto', 'HEALTHY') | list | length %}
    {% set critical_hosts = report_data | selectattr('status', 'equalto', 'CRITICAL') | list | length %}
    {% set healthy_containers = report_data | map(attribute='healthy_containers') | list | flatten | length %}
    {% set down_containers = report_data | map(attribute='down_containers') | list | flatten | length %}
    {% set missing_containers = report_data | map(attribute='missing_containers') | list | flatten | length %}

    <div class="summary">
        <div class="summary-card healthy">
            <h3>{{ healthy_hosts }}</h3>
            <p>Healthy Hosts</p>
        </div>
        <div class="summary-card critical">
            <h3>{{ critical_hosts }}</h3>
            <p>Critical Hosts</p>
        </div>
        <div class="summary-card info">
            <h3>{{ healthy_containers }}</h3>
            <p>Running Containers</p>
        </div>
        <div class="summary-card warning">
            <h3>{{ down_containers + missing_containers }}</h3>
            <p>Problem Containers</p>
        </div>
    </div>

    {% if critical_hosts == 0 %}
        <div class="no-issues">
            ✅ All AAP 2.5 components are healthy!<br>
            No missing or down containers detected.
        </div>
    {% endif %}

    {% for host_data in report_data | sort(attribute='status', reverse=true) %}
    <div class="host-section">
        <div class="host-header {{ host_data.status }}">
            <span>{{ host_data.host }}</span>
            <span style="float: right;">{{ host_data.component_type | title }} - {{ host_data.status }}</span>
        </div>
        {% if host_data.containers | length > 0 %}
        <table class="container-table">
            <thead>
                <tr>
                    <th>Container Name</th>
                    <th>Status</th>
                    <th>Start Time</th>
                </tr>
            </thead>
            <tbody>
                {% for container in host_data.containers %}
                <tr>
                    <td>{{ container.name }}</td>
                    <td>
                        {% if not container.present %}
                            <span class="status-badge status-missing">Missing</span>
                        {% elif container.state == 'running' %}
                            <span class="status-badge status-running">Running</span>
                        {% else %}
                            <span class="status-badge status-stopped">{{ container.state | title }}</span>
                        {% endif %}
                    </td>
                    <td>
                        {% if container.present and container.started_at %}
                          {{ container.started_at | to_datetime | strftime("%Y-%m-%d %H:%M:%S") }}
                        {% else %}
                          N/A
                        {% endif %}
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% endif %}
    </div>
    {% endfor %}

    <div class="footer">
        <p>
            Report generated by Ansible AAP 2.5 Container Health Check Playbook<br>
            Total Hosts Checked: {{ total_hosts }}
        </p>
    </div>
</div>
</body>
</html>
